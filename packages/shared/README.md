# `packages/shared`

Shared utilities that are not domain-specific (date helpers, small pure functions, common constants).

If a file encodes product meaning, it belongs in `packages/domain` instead.


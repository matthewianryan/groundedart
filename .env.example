# API configuration (used by the API app and Alembic migrations)
DATABASE_URL=postgresql+asyncpg://groundedart:groundedart@localhost:5432/groundedart
API_CORS_ORIGINS=http://localhost:5173
# Optional test DB override (pytest derives `<DATABASE_URL>_test` if unset)
# DATABASE_URL_TEST=postgresql+asyncpg://groundedart:groundedart@localhost:5432/groundedart_test

# Used to hash opaque session/check-in tokens stored in DB.
TOKEN_HASH_SECRET=dev-only-change-me
# Shared secret for admin moderation endpoints (header: X-Admin-Token).
ADMIN_API_TOKEN=dev-admin-token-change-me

# Session cookie configuration (review before deployment).
SESSION_COOKIE_SECURE=false
SESSION_COOKIE_SAMESITE=lax
# SESSION_COOKIE_DOMAIN=example.com

# Check-in configuration (seconds/meters).
CHECKIN_CHALLENGE_TTL_SECONDS=120
CHECKIN_TOKEN_TTL_SECONDS=600
MAX_LOCATION_ACCURACY_M=50
# Solana RPC used by the API for tip receipt verification (devnet-only demo).
# Keep this on devnet and keep it consistent with `VITE_SOLANA_RPC_URL` in the web app.
SOLANA_RPC_URL=https://api.devnet.solana.com

# Local development media storage (captures upload).
MEDIA_DIR=./.local_media
MEDIA_SERVE_STATIC=true
MEDIA_PUBLIC_BASE_URL=/media
UPLOAD_ALLOWED_MIME_TYPES=["image/jpeg","image/png","image/webp"]
UPLOAD_MAX_BYTES=1500000

# Verification workflow integration (optional).
# VERIFICATION_EVENTS_MODE=noop|log|webhook
VERIFICATION_EVENTS_MODE=log
# VERIFICATION_EVENTS_WEBHOOK_URL=https://example.com/groundedart/webhooks/verification-events
# VERIFICATION_EVENTS_WEBHOOK_TOKEN=change-me
# VERIFICATION_EVENTS_WEBHOOK_TIMEOUT_SECONDS=5.0

# Web app configuration
VITE_API_ORIGIN=http://localhost:8000
# Google Maps Platform key with Maps JS, Directions, Places, and Geocoding APIs enabled (HTTP referrer restricted)
VITE_GOOGLE_MAPS_API_KEY=replace-with-your-key
# Demo defaults (local-only). Set these to auto-enable demo mode and puppet controls.
VITE_DEMO_MODE=true
VITE_DEMO_PUPPET_ENABLED=true
VITE_DEMO_AUTO_VERIFY=true
VITE_DEMO_PUPPET_ACCURACY_M=8
VITE_DEMO_CLICK_TO_MOVE=true
# Demo-only: expose the admin token to the web client (enables auto-verify + rank-up in demo mode).
# Never set this for a public deployment.
# VITE_ADMIN_API_TOKEN=dev-admin-token-change-me
# Tip UI toggle (Solana devnet-only demo): set true to show tipping UX.
VITE_TIPS_ENABLED=false
# Solana RPC used by the web wallet adapter (devnet-only demo).
# Keep this on devnet and keep it consistent with `SOLANA_RPC_URL` used by the API.
VITE_SOLANA_RPC_URL=https://api.devnet.solana.com

# Docker compose defaults live in `infra/docker-compose.yml`.
# Add overrides here if needed.

services:
  db:
    image: kartoza/postgis:16-3.4
    environment:
      POSTGRES_DB: groundedart
      POSTGRES_USER: groundedart
      POSTGRES_PASSWORD: groundedart
    ports:
      - "5432:5432"
    volumes:
      - groundedart_pgdata:/var/lib/postgresql/data
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "PGPASSWORD=$${POSTGRES_PASSWORD} pg_isready -h $(hostname -i) -U $${POSTGRES_USER} -d $${POSTGRES_DB}",
        ]
      interval: 5s
      timeout: 5s
      retries: 5

  api:
    build:
      context: ..
      dockerfile: apps/api/Dockerfile
    environment:
      DATABASE_URL: postgresql+asyncpg://groundedart:groundedart@db:5432/groundedart
      API_CORS_ORIGINS: http://localhost:5173
      TOKEN_HASH_SECRET: dev-only-change-me
      MEDIA_DIR: /app/.local_media
    ports:
      - "8000:8000"
    volumes:
      - ..:/app
      - api_media:/app/.local_media
    working_dir: /app/apps/api
    depends_on:
      migrate:
        condition: service_completed_successfully
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import httpx; httpx.get('http://127.0.0.1:8000/health', timeout=2.0).raise_for_status()",
        ]
      interval: 5s
      timeout: 3s
      retries: 20
    command: uvicorn groundedart_api.main:app --host 0.0.0.0 --port 8000 --reload

  web:
    build:
      context: ..
      dockerfile: apps/web/Dockerfile
    env_file:
      - ../.env
    ports:
      - "5173:5173"
    volumes:
      - ../apps/web:/app/apps/web
      - web_node_modules:/app/apps/web/node_modules
    working_dir: /app/apps/web
    depends_on:
      api:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:5173/').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))",
        ]
      interval: 5s
      timeout: 3s
      retries: 20
    command: npm run dev -- --host 0.0.0.0 --port 5173

  tip-reconciler:
    build:
      context: ..
      dockerfile: apps/api/Dockerfile
    environment:
      DATABASE_URL: postgresql+asyncpg://groundedart:groundedart@db:5432/groundedart
      SOLANA_RPC_URL: https://api.devnet.solana.com
      TIP_RECEIPT_RECONCILIATION_INTERVAL_SECONDS: 60
      TIP_RECEIPT_RECONCILIATION_CUTOFF_SECONDS: 3600
    volumes:
      - ..:/app
    working_dir: /app/apps/api
    depends_on:
      migrate:
        condition: service_completed_successfully
    command: python scripts/reconcile_tip_receipts.py --loop

  migrate:
    build:
      context: ..
      dockerfile: apps/api/Dockerfile
    environment:
      DATABASE_URL: postgresql+asyncpg://groundedart:groundedart@db:5432/groundedart
      MEDIA_DIR: /app/.local_media
    volumes:
      - ..:/app
      - api_media:/app/.local_media
    working_dir: /app/apps/api
    depends_on:
      db:
        condition: service_healthy
    command: sh -c "alembic -c alembic.ini upgrade head && python scripts/seed_nodes.py && python scripts/seed_artists.py && python scripts/seed_node_images.py"

volumes:
  groundedart_pgdata:
  api_media:
  web_node_modules:
